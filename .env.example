# Supabase (Project Settings > API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=        # secreto — solo en server actions del admin

# Panel /admin
ADMIN_PASSWORD=                   # contraseña del panel
ADMIN_SESSION_TOKEN=              # secreto aleatorio para firmar la sesión (HMAC)

# Opcional: URL pública del sitio (para Open Graph / metadata)
NEXT_PUBLIC_SITE_URL=https://quiniela-hunters.vercel.app
